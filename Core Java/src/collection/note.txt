collection:
myArrayList是通过数组来保存数据的
1、增加操作
   直接增加到最后一个位置上，只操作一次
   加到第一个位置上，操作size次（size表示元素个数）
 （size+1）/2
 2、删除操作
  
  
  
  基于数组的数据结构：增删性能比较差，改查性能比较好
  链表（链接列表）：分单向链表和双向链表
  
  任何集合类都包含了三大块内容：
  对外的接口
  接口的实现
  
  ArrayList和vector底层的数据结构是数组，所以改查快，增删慢
  
